<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <!-- Customize your theme here. -->
		<item type="attr" name="android:colorPrimary">@android:color/black</item>
		<item type="attr" name="android:windowBackground">@android:color/black</item>
    </style>

    <style name="SplashTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
		<item type="attr" name="android:colorPrimary">@android:color/black</item>
		<item type="attr" name="android:windowBackground">@android:color/black</item>
        <!--<item name="android:windowBackground">@drawable/background_splash</item>--> <!-- currently splashscreen plugin takes care of the image -->
    </style>

</resources>
